<!DOCTYPE suite SYSTEM "https://testng.org/testng-1.0.dtd" >
<suite name="hapifyMe API Test Suite">
    <!-- Adaugă secțiunea de listeners -->
    <listeners>
        <listener class-name="listeners.ExtentReportListener"/>
    </listeners>

    <test name="All Tests">
        <packages>
            <package name="tests"/>
        </packages>
    </test>
</suite>