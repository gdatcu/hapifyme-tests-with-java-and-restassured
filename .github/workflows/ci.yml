name: API Test Automation CI

# Declanseaza workflow-ul la fiecare push pe branch-ul 'main'
on:
  push:
    branches: [ "main" ]

jobs:
  build-and-test:
    # Ruleaza pe o masina virtuala Ubuntu oferita de GitHub
    runs-on: ubuntu-latest

    steps:
      # Pasul 1: Descarca codul din repository
      - name: Check out repository code
        uses: actions/checkout@v3

      # Pasul 2: Seteaza mediul de Java (versiune 17)
      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'

      # Pasul 3: Ruleaza testele folosind Maven
      - name: Run tests with Maven
        run: mvn clean test

      # Pasul 4 (Optional): Incarca raportul ca artefact
      - name: Upload test report
        # Ruleaza acest pas doar daca testele reusesc sau esueaza, nu daca job-ul e anulat
        if: always()
        uses: actions/upload-artifact@v3
        with:
          name: extent-report
          path: reports/

